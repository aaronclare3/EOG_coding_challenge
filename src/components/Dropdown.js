import React from 'react';

const Dropdown = () => {
  return (
    <div>
      <select>
        <option defaultValue="Select a Metric" hidden>
          Select a metric
        </option>
      </select>
    </div>
  );
};

export default Dropdown;
